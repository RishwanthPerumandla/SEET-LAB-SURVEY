// src/components/Home.jsx
import { Button } from '@mui/material';

const Home = () => {
    return (
        <div>
            <h1>Welcome Home!</h1>
            <Button variant="contained" color="primary">
                Test Button
            </Button>
        </div>
    );
};

export default Home;
